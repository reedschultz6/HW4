<h1 class="mb-4">Places</h1>

<% if session["user_id"] %>
<div class="card shadow-sm p-3">
<ul class="list-group list-group-flush"> 
<% @places.each do |place| %>
<% if place["name"].present? %> 
<li class="list-group-item py-2"> 
<a href="/places/<%= place["id"] %>" class="text-decoration-none fw-bold text-primary">
<%= place["name"] %>
</a>
</li>
<% end %>
<% end %>
</ul>
</div>

<div class="mt-4"> 
<a href="/places/new" class="btn btn-outline-secondary">➕ Add New Place </a>
</a></div>

<% else %>
<div class="alert alert-warning mt-4">
You must be logged in to view places.
</div>
<% end %>
